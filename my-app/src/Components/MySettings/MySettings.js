import c from './MySettings.module.css'

const MySettings = () => {
    return (
    <div className={c.settings}>
    Change password
        change email adress
        </div>
    );
}

export default MySettings;