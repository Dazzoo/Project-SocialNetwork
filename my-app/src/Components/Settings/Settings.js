import c from './Settings.module.css'

const Settings = () => {
    return (
    <div className={c.settings}>
    Change password
        change email adress
        </div>
    );
}

export default Settings;